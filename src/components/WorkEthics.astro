---
/* src/components/WorkEthics.astro */
---

<section id="work-ethics" class="relative overflow-hidden bg-[#0A0B0D] text-neutral-100 py-16 lg:py-20">
  <!-- Ambient accents (subtle, no custom CSS) -->
  <div aria-hidden="true" class="pointer-events-none absolute inset-0">
    <div class="absolute -top-24 -right-16 h-56 w-56 rounded-full bg-amber-500/10 blur-2xl"></div>
    <div class="absolute bottom-0 left-10 h-32 w-32 rounded-full bg-amber-500/10 blur-xl"></div>
  </div>

  <div class="relative z-10 mx-auto max-w-7xl px-6 lg:px-8">
    <!-- Header -->
    <header class="mx-auto max-w-3xl text-center">
      <span class="inline-flex items-center gap-2 rounded-full border border-amber-500/20 bg-amber-500/10 px-4 py-1.5 text-amber-300 font-semibold text-xs tracking-wide reveal-up">
        <!-- emblem -->
        <svg aria-hidden="true" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 3l2.6 5.2 5.8.8-4.2 4.1 1 5.8-5.2-2.7-5.2 2.7 1-5.8L3.6 9.3l5.8-.8L12 3z"/></svg>
        Consciously Made
      </span>
      <h2 class="mt-4 font-serif text-4xl lg:text-5xl font-bold leading-tight reveal-up" style="--d:1">
        Work Ethics — The Artilate Standard
      </h2>
      <p class="mt-3 text-neutral-300 reveal-up" style="--d:2">
        We keep decisions simple, materials honest, and batches small. Quality isn’t a line item; it’s the way we work.
      </p>
    </header>

    <!-- PRINCIPLES GRID -->
    <div class="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
      <!-- 1. Sourcing -->
      <article class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-up" style="--d:1">
        <div class="flex items-start gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-lg border border-amber-500/20 bg-amber-500/10 text-amber-300">
            <!-- leaf/hand -->
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
              <path d="M8 12l3 3 5-5" /><path d="M7 7c3-3 7-3 10 0 2 2 2 6 0 8" /><path d="M4 14c-1-3 0-5 2-7" />
            </svg>
          </span>
          <div>
            <h3 class="font-serif text-xl font-semibold">Respectful Sourcing</h3>
            <p class="mt-1 text-neutral-300">Cacao is the lead. We choose lots for character; supporting ingredients stay real and minimal.</p>
          </div>
        </div>
      </article>

      <!-- 2. Process -->
      <article class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-up" style="--d:2">
        <div class="flex items-start gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-lg border border-amber-500/20 bg-amber-500/10 text-amber-300">
            <!-- curve/thermo -->
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
              <path d="M4 18c3-8 6-8 9-2s6 6 7-2" /><path d="M18 6v6" />
            </svg>
          </span>
          <div>
            <h3 class="font-serif text-xl font-semibold">Discipline in Technique</h3>
            <p class="mt-1 text-neutral-300">Hand-tempered shells, controlled emulsions, and a clean melt. We keep sweetness in check.</p>
          </div>
        </div>
      </article>

      <!-- 3. Ingredients -->
      <article class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-up" style="--d:3">
        <div class="flex items-start gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-lg border border-amber-500/20 bg-amber-500/10 text-amber-300">
            <!-- droplet -->
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
              <path d="M12 3s6 6 6 10a6 6 0 1 1-12 0c0-4 6-10 6-10z"/>
            </svg>
          </span>
          <div>
            <h3 class="font-serif text-xl font-semibold">Clean Inputs</h3>
            <p class="mt-1 text-neutral-300"><span class="font-semibold">Sunflower lecithin</span> (no soy). Real fruit, real nuts, real dairy where used. No “flavour-only” shortcuts.</p>
          </div>
        </div>
      </article>

      <!-- 4. Safety -->
      <article class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-up" style="--d:4">
        <div class="flex items-start gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-lg border border-amber-500/20 bg-amber-500/10 text-amber-300">
            <!-- shield/check -->
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
              <path d="M12 2l7 3v6c0 5-3.5 9-7 11-3.5-2-7-6-7-11V5l7-3z"/><path d="M9 12l2 2 4-4"/>
            </svg>
          </span>
          <div>
            <h3 class="font-serif text-xl font-semibold">Food Safety First</h3>
            <p class="mt-1 text-neutral-300">HACCP mindset: allergen controls, lot traceability, and documented checks on every batch.</p>
          </div>
        </div>
      </article>

      <!-- 5. Scale -->
      <article class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-up" style="--d:5">
        <div class="flex items-start gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-lg border border-amber-500/20 bg-amber-500/10 text-amber-300">
            <!-- scale icon -->
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
              <path d="M3 7h18M6 7l3 9h6l3-9"/>
            </svg>
          </span>
          <div>
            <h3 class="font-serif text-xl font-semibold">Small by Design</h3>
            <p class="mt-1 text-neutral-300">Short runs for tighter control. Typical Alberta lead time: <span class="font-semibold">under a week</span>.</p>
          </div>
        </div>
      </article>

      <!-- 6. Transparency -->
      <article class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-up" style="--d:6">
        <div class="flex items-start gap-3">
          <span class="grid h-10 w-10 place-items-center rounded-lg border border-amber-500/20 bg-amber-500/10 text-amber-300">
            <!-- document -->
            <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
              <rect x="4" y="3" width="14" height="18" rx="2"/><path d="M8 8h6M8 12h6M8 16h4"/>
            </svg>
          </span>
          <div>
            <h3 class="font-serif text-xl font-semibold">Plain Transparency</h3>
            <p class="mt-1 text-neutral-300">Ask us what’s inside and why. We answer in ingredients, not marketing.</p>
          </div>
        </div>
      </article>
    </div>

    <!-- DO / DON'T (very clear, no fluff) -->
    <div class="mt-10 grid gap-6 lg:grid-cols-2">
      <div class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-left" style="--d:1">
        <h4 class="font-serif text-lg font-semibold">We Do</h4>
        <ul class="mt-3 space-y-2 text-neutral-300">
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-amber-300">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
            </span>
            Hand-painted shells, manual temper curves, tight water activity targets.
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-amber-300">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
            </span>
            Real fruit purées, pralinés, nut pastes; <span class="font-semibold">sunflower lecithin</span>.
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-amber-300">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6L9 17l-5-5"/></svg>
            </span>
            Batch traceability, allergen zoning, clean-down logs.
          </li>
        </ul>
      </div>

      <div class="rounded-2xl border border-amber-500/15 bg-black/30 p-6 reveal-right" style="--d:1">
        <h4 class="font-serif text-lg font-semibold">We Don’t</h4>
        <ul class="mt-3 space-y-2 text-neutral-300">
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-amber-300">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </span>
            No soy lecithin. No “flavour-only” stand-ins for fruit or nuts.
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-amber-300">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </span>
            No oversized runs that compromise temper, gloss, or snap.
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-0.5 text-amber-300">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </span>
            No vague language about sourcing or allergens.
          </li>
        </ul>
      </div>
    </div>

    <!-- CTA Buttons con estilos Artilate -->
    <div class="mt-12 flex justify-center gap-4 reveal-up" style="--d:2">
      <a href="/about" class="primary-btn">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
        </svg>
        Learn More About Us
      </a>

      <a href="/contact" class="secondary-btn">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M21 10c0 6-9 13-9 13S3 16 3 10a9 9 0 1 1 18 0z"/>
          <circle cx="12" cy="10" r="3"/>
        </svg>
        Get in Touch
      </a>
    </div>

    <!-- Footnote / compliance -->
    <footer class="mt-8 text-xs text-neutral-400 text-center reveal-up" style="--d:3">
      Contains milk; selected fillings contain tree nuts; may contain traces of peanuts/other nuts. Made with sunflower lecithin (no soy).
      For full details see Ingredients & Allergens.
    </footer>
  </div>
</section>

<style >
  /* Revels globales coherentes con el sitio */
  :root{ --ease:cubic-bezier(.16,1,.3,1); }
  .reveal-up,.reveal-left,.reveal-right{
    opacity:0;
    will-change:transform,opacity,filter;
    transition:opacity .7s var(--ease),transform .7s var(--ease),filter .7s var(--ease);
    transition-delay:calc(var(--d,0)*90ms);
    filter:blur(2px);
  }
  .reveal-up{ transform:translateY(18px) scale(.985); }
  .reveal-left{ transform:translateX(-22px); }
  .reveal-right{ transform:translateX(22px); }
  .is-visible{ opacity:1; transform:none; filter:none; }
</style>

<script is:inline>
  // Scroll reveal (mismo patrón del resto del sitio)
  (function initReveals(){
    const els = document.querySelectorAll('#work-ethics .reveal-up, #work-ethics .reveal-left, #work-ethics .reveal-right');
    if (!els.length) return;

    const io = new IntersectionObserver((entries) => {
      for (const e of entries) {
        if (e.isIntersecting) {
          e.target.classList.add('is-visible');
          io.unobserve(e.target);
        }
      }
    }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });

    els.forEach(el => io.observe(el));
  })();
</script>
