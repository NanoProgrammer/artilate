---
import CardWithProvider from '../components/CardWithProvider.jsx';
const { imgUrl, title, description, id , price} = Astro.props;
---

<CardWithProvider 
  imgUrl={imgUrl}
  title={title}
  description={description}
  id={id}
  price={price}
  client:load
/>


