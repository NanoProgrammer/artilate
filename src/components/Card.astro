---
import CardWithProvider from '../components/CardWithProvider.jsx';
const { imgUrl, title, description, id } = Astro.props;
---

<CardWithProvider 
  imgUrl={imgUrl}
  title={title}
  description={description}
  id={id}
  client:load
/>
