---
import Card from '../components/Card.jsx';

const { imgUrl, title, description, id } = Astro.props;
---

<Card 
  imgUrl={imgUrl}
  title={title}
  description={description}
  id={id}
  client:only="Card"
/>
