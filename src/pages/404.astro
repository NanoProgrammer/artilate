---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const pageTitle = "Page Not Found | Artilate";
---

<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{pageTitle}</title>
  <meta name="description" content="Oops! The page you're looking for seems to have melted away like chocolate in the sun.">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body>
  <Header />
  
  <main class="error-page">
    <div class="container">
      <div class="error-content">
        <!-- Animated Chocolate Crack Effect -->
        <div class="chocolate-crack">
          <svg viewBox="0 0 800 200" class="crack-svg">
            <path 
              class="crack-line" 
              d="M50,100 Q200,80 300,100 T500,120 Q600,110 750,100" 
              stroke="url(#crackGradient)" 
              stroke-width="3" 
              fill="none"
            />
            <path 
              class="crack-line secondary" 
              d="M100,90 Q250,105 400,95 T650,85" 
              stroke="url(#crackGradient)" 
              stroke-width="2" 
              fill="none"
            />
            <path 
              class="crack-line tertiary" 
              d="M200,110 Q350,125 500,115 T700,105" 
              stroke="url(#crackGradient)" 
              stroke-width="1.5" 
              fill="none"
            />
            
            <defs>
              <linearGradient id="crackGradient" x1="0%" y1="0%" x2="100%" y2="0%">
                <stop offset="0%" style="stop-color:#D4A373;stop-opacity:0" />
                <stop offset="20%" style="stop-color:#D4A373;stop-opacity:0.8" />
                <stop offset="50%" style="stop-color:#F59E0B;stop-opacity:1" />
                <stop offset="80%" style="stop-color:#D4A373;stop-opacity:0.8" />
                <stop offset="100%" style="stop-color:#D4A373;stop-opacity:0" />
              </linearGradient>
            </defs>
          </svg>
        </div>

        <!-- Main Error Content -->
        <div class="error-text">
          <div class="error-number">
            <span class="digit">4</span>
            <div class="melting-zero">
              <svg viewBox="0 0 120 120" class="zero-svg">
                <circle 
                  cx="60" 
                  cy="60" 
                  r="40" 
                  stroke="currentColor" 
                  stroke-width="8" 
                  fill="none" 
                  class="zero-circle"
                />
                <!-- Melting drops -->
                <circle cx="60" cy="100" r="3" fill="currentColor" class="drop drop-1" />
                <circle cx="50" cy="105" r="2" fill="currentColor" class="drop drop-2" />
                <circle cx="70" cy="108" r="2.5" fill="currentColor" class="drop drop-3" />
              </svg>
            </div>
            <span class="digit">4</span>
          </div>

          <h1 class="error-title">This Page Has Melted Away</h1>
          
          <p class="error-description">
            Like chocolate left in the sun, the page you're looking for seems to have disappeared. 
            Don't worry—our finest chocolates are still perfectly crafted and waiting for you.
          </p>

          <div class="error-actions">
            <a href="/" class="primary-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                <polyline points="9,22 9,12 15,12 15,22"/>
              </svg>
              Return Home
            </a>
            
            <a href="/shop" class="secondary-btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/>
                <line x1="3" y1="6" x2="21" y2="6"/>
                <path d="M16 10a4 4 0 0 1-8 0"/>
              </svg>
              Browse Chocolates
            </a>
          </div>

          <!-- Suggested Links -->
          <div class="suggested-links">
            <h3>Or explore these popular pages:</h3>
            <nav class="quick-links">
              <a href="/about" class="quick-link">Our Story</a>
              <a href="/journal" class="quick-link">Journal</a>
              <a href="/find-us" class="quick-link">Find Us</a>
              <a href="/contact" class="quick-link">Contact</a>
            </nav>
          </div>
        </div>

        <!-- Floating Chocolate Particles -->
        <div class="chocolate-particles">
          <div class="particle particle-1"></div>
          <div class="particle particle-2"></div>
          <div class="particle particle-3"></div>
          <div class="particle particle-4"></div>
          <div class="particle particle-5"></div>
          <div class="particle particle-6"></div>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</body>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  :root {
    --cacao-black: #0A0B0D;
    --rich-black: #141618;
    --warm-ivory: #FAFAF9;
    --gold: #D4A373;
    --emerald: #047857;
    --violet: #5B21B6;
    --amber: #F59E0B;
    --warm-gray: rgba(250, 250, 249, 0.8);
  }

  body {
    font-family: 'Inter', system-ui, sans-serif;
    background: var(--cacao-black);
    color: var(--warm-ivory);
    line-height: 1.6;
    overflow-x: hidden;
    padding-top: 84px;
  }

  .container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Error Page Layout */
  .error-page {
    min-height: calc(90vh);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    padding-bottom: 32px;
    background: 
      radial-gradient(ellipse at 30% 20%, rgba(91, 33, 182, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse at 70% 80%, rgba(245, 158, 11, 0.06) 0%, transparent 50%);
  }

  .error-content {
    text-align: center;
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  /* Chocolate Crack Animation */
  .chocolate-crack {
    margin-bottom: 1rem;
    opacity: 0;
    animation: crackAppear 1s ease-out 0.5s forwards;
  }

  .crack-svg {
    width: 100%;
    max-width: 600px;
    height: 120px;
  }

  .crack-line {
    stroke-dasharray: 1000;
    stroke-dashoffset: 1000;
    animation: drawCrack 2s ease-out 0.8s forwards;
  }

  .crack-line.secondary {
    animation-delay: 1.2s;
    opacity: 0.7;
  }

  .crack-line.tertiary {
    animation-delay: 1.6s;
    opacity: 0.4;
  }

  @keyframes crackAppear {
    to { opacity: 1; }
  }

  @keyframes drawCrack {
    to { stroke-dashoffset: 0; }
  }

  /* Error Number */
  .error-number {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 2rem;
    opacity: 0;
    transform: translateY(30px);
    animation: slideUp 0.8s ease-out 1s forwards;
  }

  .digit {
    font-family: 'Playfair Display', serif;
    font-size: clamp(6rem, 15vw, 12rem);
    font-weight: 700;
    color: var(--gold);
    text-shadow: 0 4px 20px rgba(212, 163, 115, 0.3);
    position: relative;
  }

  .digit::before {
    content: attr(data-text);
    position: absolute;
    top: 0;
    left: 0;
    background: linear-gradient(135deg, var(--gold) 0%, var(--amber) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Melting Zero */
  .melting-zero {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .zero-svg {
    width: clamp(6rem, 15vw, 12rem);
    height: clamp(6rem, 15vw, 12rem);
    color: var(--gold);
  }

  .zero-circle {
    stroke-dasharray: 251;
    stroke-dashoffset: 251;
    animation: drawZero 1.5s ease-out 1.5s forwards;
  }

  .drop {
    opacity: 0;
    animation: dropFall 2s ease-out infinite;
  }

  .drop-1 { animation-delay: 2s; }
  .drop-2 { animation-delay: 2.3s; }
  .drop-3 { animation-delay: 2.6s; }

  @keyframes drawZero {
    to { stroke-dashoffset: 0; }
  }

  @keyframes dropFall {
    0% { opacity: 0; transform: translateY(-10px); }
    50% { opacity: 1; }
    100% { opacity: 0; transform: translateY(20px); }
  }

  /* Error Text */
  .error-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 600;
    color: var(--warm-ivory);
    margin-bottom: 1.5rem;
    line-height: 1.2;
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 0.8s ease-out 1.8s forwards;
  }

  .error-description {
    font-size: 1.125rem;
    color: var(--warm-gray);
    max-width: 500px;
    margin: 0 auto 3rem;
    line-height: 1.7;
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 0.8s ease-out 2s forwards;
  }

  @keyframes slideUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Action Buttons */
  .error-actions {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 4rem;
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 0.8s ease-out 2.2s forwards;
  }

  .primary-btn {
    background: linear-gradient(135deg, var(--gold) 0%, var(--amber) 100%);
    color: var(--cacao-black);
    padding: 16px 32px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    font-size: 16px;
    display: inline-flex;
    align-items: center;
    gap: 12px;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    box-shadow: 
      0 8px 32px rgba(212, 163, 115, 0.3),
      0 2px 16px rgba(0, 0, 0, 0.1);
    position: relative;
    overflow: hidden;
  }

  .primary-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.6s ease;
  }

  .primary-btn:hover::before {
    left: 100%;
  }

  .primary-btn:hover {
    transform: translateY(-3px);
    box-shadow: 
      0 12px 40px rgba(212, 163, 115, 0.4),
      0 4px 20px rgba(0, 0, 0, 0.15);
  }

  .secondary-btn {
    background: rgba(212, 163, 115, 0.1);
    border: 1px solid rgba(212, 163, 115, 0.2);
    color: var(--warm-ivory);
    padding: 16px 32px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    font-size: 16px;
    display: inline-flex;
    align-items: center;
    gap: 12px;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    backdrop-filter: blur(8px);
    position: relative;
    overflow: hidden;
  }

  .secondary-btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(212, 163, 115, 0.05);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .secondary-btn:hover::before {
    opacity: 1;
  }

  .secondary-btn:hover {
    border-color: rgba(212, 163, 115, 0.4);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
  }

  /* Suggested Links */
  .suggested-links {
    opacity: 0;
    transform: translateY(20px);
    animation: slideUp 0.8s ease-out 2.4s forwards;
  }

  .suggested-links h3 {
    font-size: 1rem;
    color: var(--warm-gray);
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .quick-links {
    display: flex;
    gap: 2rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .quick-link {
    color: rgba(212, 163, 115, 0.8);
    text-decoration: none;
    font-size: 15px;
    font-weight: 500;
    transition: all 0.3s ease;
    position: relative;
    padding: 8px 0;
  }

  .quick-link::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 0;
    width: 0;
    height: 1px;
    background: var(--gold);
    transition: width 0.3s ease;
  }

  .quick-link:hover {
    color: var(--gold);
  }

  .quick-link:hover::after {
    width: 100%;
  }

  /* Floating Particles */
  .chocolate-particles {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .particle {
    position: absolute;
    width: 6px;
    height: 6px;
    background: var(--gold);
    border-radius: 50%;
    opacity: 0.3;
    animation: float 6s ease-in-out infinite;
  }

  .particle-1 { top: 20%; left: 10%; animation-delay: 0s; }
  .particle-2 { top: 60%; left: 85%; animation-delay: 1s; }
  .particle-3 { top: 80%; left: 20%; animation-delay: 2s; }
  .particle-4 { top: 30%; left: 70%; animation-delay: 3s; }
  .particle-5 { top: 10%; left: 60%; animation-delay: 4s; }
  .particle-6 { top: 70%; left: 40%; animation-delay: 5s; }

  @keyframes float {
    0%, 100% { transform: translateY(0) scale(1); opacity: 0.3; }
    50% { transform: translateY(-20px) scale(1.2); opacity: 0.6; }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .error-actions {
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    .primary-btn,
    .secondary-btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }

    .quick-links {
      gap: 1.5rem;
    }

    .crack-svg {
      height: 80px;
    }
  }

  @media (max-width: 480px) {
    body {
      padding-top: 74px;
    }

    .error-page {
      min-height: calc(90vh - 74px);
      padding: 2rem 0;
    }

    .container {
      padding: 0 1rem;
    }

    .error-number {
      gap: 0.5rem;
    }

    .error-description {
      font-size: 1rem;
    }

    .quick-links {
      flex-direction: column;
      gap: 1rem;
      align-items: center;
    }
  }
</style>

<script>
 
</script>
</html>